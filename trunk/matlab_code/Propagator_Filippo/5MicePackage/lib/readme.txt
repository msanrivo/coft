If you are running Linux 64 bit, you should copy the contents of "Linux" folder into this folder

If you are running Windows 32 bit, you should copy the contents of "w32bit" folder into this folder

If you are running Windows 64 bit or Mac 64 bit, all is set fine